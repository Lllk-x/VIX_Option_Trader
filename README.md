# VIX_Option_Trader
A Python pipeline to collect VIX option-chain snapshots via yfinance, compute implied-vol “wing” signals, generate VIX call-spread trades, and backtest performance (return, Sharpe, drawdown) from saved snapshots.
